# Template artwork update

Reuse the existing overlay menu and button styles. Place the artwork action below appearance controls, where it remains separate from display settings. Use the same full label in the template tree context menu. Allow wrapping on narrow screens. Disable the button during capture and save; show outcomes through existing notifications.
