node_modules/
/data/
dist/
.turbo/
.svelte-kit/
*.tsbuildinfo

.env
.env.*
!.env.example

.DS_Store
.wrangler/
.codex-mutants/
.pnpm-store/
test-results/
/images/

# Agent scratch space. Subagent git worktrees land in .claude/worktrees/, and each one carries its
# own biome.json — which biome reads as a nested root configuration and refuses to run against.
.claude/

# Local secrets for wrangler dev
.dev.vars
.dev.vars.*

# Locally rendered share previews
/.scratch/social-images/

# Verified canvas images and local historical accounting repair plans
/.scratch/progress-recount/
