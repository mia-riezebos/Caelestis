apiVersion: v1
kind: Service
metadata:
  name: {{ include "caelestis.name" . }}
spec:
  selector: {{ include "caelestis.labels" . | nindent 4 }}
  ports:
    - { name: http, port: {{ .Values.service.port }}, targetPort: http }
