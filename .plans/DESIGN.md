# Template tags

Use the existing tree action buttons and one shared native dialog for Local and server tags.
The dialog names its owning browser or server. Template rows open assignment checkboxes; root rows open the reusable catalog.
Reuse the UI package's typography, buttons, and theme tokens. Keep rows compact and aligned.
Rename inline. Confirm deletion inline with its effect on every assignment. Keep failed input editable and offer reload after connection failures.
Implement directly in the production components. Verify through background CDP tabs in the existing Chromium instance.
