# #311 Explore region claims and live painter viewport indicators

## Summary
Painters on the same artwork cannot see where the others are. Add a Google Docs style presence
channel over WebSockets: each online painter shares the world-pixel rect their viewport covers and
the rect (plus a small bitmask) of the pixels they have drafted. Painters can also claim a region of
a template; claims are the only thing stored in the database. The userscript draws all three under
the template layer as low-opacity tinted rects so finished art covers them. Everything is toggleable.

## Acceptance criteria
- [ ] Other online painters' viewports draw beneath the artwork as a dashed, very low opacity rect in their colour.
- [ ] A painter with an open draft draws as a solid, slightly more opaque rect over the drafted pixels' bounds; drafted pixels inside it are tinted.
- [ ] Region claims persist in D1, list per template, and draw as a solid low-opacity region with the claimant's name.
- [ ] A painter can claim the region they are looking at, or their draft's bounds, and release their own claims.
- [ ] Indicators disappear within a few seconds of the painter closing the tab or losing the socket.
- [ ] Sharing my viewport and showing others' indicators are separate toggles that persist.
- [ ] Traffic stays bounded: one upstream message per 1 s at most while panning, 1 s while drafting, a 30 s heartbeat otherwise; each downstream tick carries at most 64 nearby peers.

## TODOs
- [x] Shared presence contract: types, limits, rect helpers, and mask encoding in `packages/shared/src/presence.ts` with tests.
- [x] Backend (Codex): `PresenceObject` Durable Object with hibernating sockets, `/telemetry/presence` upgrade route, wire-schema events, interest-managed 1 s broadcast tick, region claim table plus `/work/regions` routes, tests.
- [x] Userscript presence client: open the socket per connected server, throttle viewport and draft publishing, keep peer state, and reconnect.
- [x] Userscript presence GL layer: draw peer viewports, drafts, and region claims beneath the template layer.
- [x] Userscript controls: share and show toggles in the panel, claim and release region actions, claimant labels.
- [x] Changesets for backend and userscript; run backend, userscript, and shared tests, typecheck, and lint.

### Review round 2: a region claim tool with shapes
- [x] Shared `RegionShape` contract (rectangle, ellipse, polygon with corner count, star with point count and inner radius) as whole-pixel sets: scanline rasteriser, bounds, pixel hit test, translate, with tests; claims carry a shape and a server-derived bounding rect.
- [~] Backend (Codex): shape column and migration, wire schemas, shape validation on `PUT /work/regions/:id`, same-claimant update on re-put, tests.
- [x] Userscript claim rendering: claims and the tool preview rasterise to nearest-sampled masks with crisp edge pixels.
- [x] Userscript claim tool: a mode that works in explore and paint, captures pointer input over the map, draws the chosen shape by drag, selects your saved claims on click, moves them with ⌘ or Ctrl drag, exposes resize and inner-radius handles, Enter saves, Delete removes, Escape leaves.
- [x] Claim tool toolbar element in `@caelestis/ui`: shape picker, corner and point inputs, pixel count, save, delete, cancel; entry from the In progress drawer and the M and L keys.
- [x] Tests for the tool (8), presence layer order (4), shape maths (6); typecheck, lint, full suites.

## Notes
- The existing `/telemetry/live` socket in `StatusReadModelObject` is capped at 256 subscribers per season and already carries tile uploads. Presence gets its own Durable Object keyed by season and surface so it can hold thousands of hibernating sockets without touching the status model.
- Cloudflare bills incoming hibernated WebSocket messages at 20:1 against requests and does not bill outgoing ones, so the client is the throttle point. Server ticks batch changes and only send peers whose rect intersects the subscriber's viewport padded by one tile.
- Draft masks live only in Durable Object memory. After hibernation the 30 s client heartbeat restores them; attachments carry rects only, keeping under the 2 KiB attachment limit.
- Region selection reuses what the painter already has: the current viewport or the draft bounds. No new drag gesture.
- Codex runs with `gpt-6-astra` on the backend while I build the userscript side. It does not commit; I review the diff and commit it.
- The userscript has its own `ServerInfo` parser in `server-manifest.ts`; the `presence` flag had to be added there as well as in shared, or the capability was dropped on read.
- Mia's round-2 review: region claims are a tool with shapes (rectangle, ellipse, polygon, star), usable in explore and draft; M opens it with a rectangle and L with an ellipse; hover, click to select, ⌘-drag to move, Delete removes; shapes must be nearest-neighbour and aliased with no half pixels. L used to toggle the Wplace theme; that moved to N.
- Pixel exactness: every shape is defined by integers and rasterised by pixel-centre membership in shared; the GL layer uploads that mask as an R8 texture with nearest sampling, so the tint stops on pixel edges. Circles became ellipses defined by a whole-pixel box for the same reason.
- Mia's round-10 review: viewports should feel more responsive and glide rather than teleport; name tags sit above their target and show only on hover; every self-contained piece of a claim gets its own tag, merged with neighbours whose tags would collide and split again when zoomed in. Done: `PRESENCE_VIEWPORT_MIN_MS` is 1 s (was 2 s), and the layer eases each maskless rect from where it was drawn to its new rect over one publish interval, requesting frames while any is moving, so a steadily panning peer never stops; drafts and claims snap because their texture is cut to the rect. Tags: `presence-labels.ts` watches the pointer over the map canvas, maps it to a canvas pixel, and tags only what it is over: a peer's draft or viewport (at the drawn, mid-glide rect) and the hovered piece of a claim. Pieces come from `regionPixelComponents` in shared (8-connected components of the rasterised mask, largest first, with a label map for the hit test). The pieces whose chips would overlap on screen, transitively, share one chip above their union. Chips are centred above the target and stack upward on collision.
- Mia's round-9 review: one presence socket per tab, not one per server, chosen by load so busy servers get fewer collaboration sockets and painters who share several servers are not seen twice; a single key (M) opens claim mode; Q hides and shows other painters and claims. Client: before opening, every presence server is asked `GET /telemetry/presence/online` (Codex round 5 adds it: one Durable Object read, no tick), the fewest-online wins, ties by server id so painters sharing servers converge; an open socket is never moved, and a lost socket re-elects after the usual backoff. Servers without the socket still contribute their persistent claims over `GET /work/regions`, fetched on election, after each save or release there, and every five minutes. L is unbound at the top level (it stays the ellipse tool inside claim mode); `showPresence` is what Q flips.
- Mia's round-8 review: the claim outline must be a literal stroke of stable thickness, never whole pixels; and the colour must be the one Wplace shows for a painter. Outline: the shader samples the mask a stroke-width away in device pixels (8 directions, texture bounds count as outside) and strokes inside the boundary at 1.5 device px. Colour: Wplace's chunk `Cm7_KSBp.js` assigns `text-{red,orange,yellow,lime,emerald,teal,cyan,sky,indigo,violet,purple,fuchsia,pink,rose}-500` by `id % 14`; the sRGB bytes were read from the live page's computed oklch values and now sit in `PRESENCE_COLOURS` in that order.
- Mia's round-7 review (with screenshot): no preview while drawing, claims not showing, appearance wrong. Cause: claims and the editor preview were drawn under the pixel art, invisible on a painted area; Mia confirms everything should sit over the art, like the overlay. Now one presence layer just under Wplace's crosshair. Appearance per spec: painter colour is `id % 14` into a named list; claims and painting viewports are solid edge, 30% fill, diagonal stripes; browsing viewports are dashed edge, 15% fill, dotted grid; drafted pixels solid in the same colour. The editor outlines the shape being dragged, and clicking one of your saved claims in claim mode loads it (refused with a message while unsaved shapes exist). The own-claims-outline-only rule is gone; the spec supersedes it.
- Mia's round-6 review: not a tool with handles but a claim *mode*, Illustrator-style, with a tool drawer on the left and a cancel-or-confirm bar on top; a vector editor with rasterisation so every shape and brush stroke stays editable, with bezier handles and a pen tool; explore mode stays hands-off. The contract becomes a `RegionDocument` of items (rectangle, ellipse, polygon, star, bezier path with stroke width), each `add` or `subtract`, rasterised in order by pixel-centre membership; `RegionClaim.document` replaces `shape`. The editor (`claim-editor.ts`) watches pointer events at the window only while active, draws outlines and handles in an SVG overlay, and renders the working document through the presence layer as a mask. Tools: V select (click, drag to move, corner or radius handles), A direct selection (anchors and mirrored bezier handles), P pen (click corners, drag curves, click the first anchor to close, Enter for an open stroke), N pencil (1 px), B brush (width option), M rectangle, L ellipse, polygon, star; Subtract toggle; Delete removes a shape; Escape leaves. The backend (Codex round 4) stores the document JSON in the existing `shape` column and wraps legacy single-shape rows.
- Mia's round-5 review: zoom must work with a tool selected; the tool switches off after a save; own claims are editable with no tool selected (click for handles, drag to move) in explore mode only, and anything off a claim pans as normal. Fix: no capture layer at all. Pointer events are watched at the window in the capture phase like template placement and consumed only for a draw drag or a press on your own claim; the wheel is never touched. Three modes: off, draw, edit. Also found via Mia's screenshot: the save went to the overlapping template's server rather than a presence-connected one; the target is now chosen among presence servers, with the template kept as a hint only when it lives on that server. Verified over CDP with the userscript injected: drag draws without panning, wheel zooms with the tool open, Escape closes. The tool is on the debug API as `__caelestis.claimTool` for that kind of check.
- Mia's round-4 review: the claim tool needs a visible entry (it was buried in a collapsed drawer that only rendered once presence connected). It now has a shapes button on the rail next to the panel, colour, and marker buttons, disabled with a reason until presence is connected and the painter is signed in, pressed while the tool is open. The "In progress" drawer is renamed "Favourites" since template claims and region claims are unrelated; presence and region claims moved into their own "Painters" drawer.
- Region claims stand alone: no template is required, `templateId` is a nullable hint. The userscript picks the server from an overlapping template when several are connected, else the first presence server; the backend (Codex round 3) makes `template_id` nullable and only validates it when given.
- Theme key: L had been dead because Wplace moved "Dark mode" from the button's `aria-label` into its text, inside the settings dialog. Mia asked for their own setter instead: Wplace's global state object (theme, language, dialogs, map) has a `theme` accessor, but lives in a module closure. Its private fields compile to `WeakMap.prototype.set(this, …)`, so `wplace-state.ts` traps that at document-start, recognises the instance by its `theme` setter, and disarms (after 20 s if it never appears). `wplace-theme.ts` assigns `state.theme` first, then clicks a visible control, then drives the settings dialog under a hiding stylesheet, then sets `data-theme` plus the `theme` storage key and swaps the map style URL (`maps.wplace.live/styles/liberty|fiord`). Verified on the live page over CDP with the real userscript injected: the state is caught on its 37th private write, N flips `custom-winter` → `dark` → back, zero dialogs opened.
- The captured state also exposes `map`, which could one day replace the `Object.prototype` trap in `map-handle.ts`. Not touched here.
- Found while verifying: `server-sync-coordinator.ts` closes sockets with codes 1002 and 1011, which browsers reject with InvalidAccessError, so those closes never happen. Pre-existing (from 33b2fbcf), left alone here; the presence client uses 4002.
- Mia's round-3 review: your own claims draw as outline only, under the art, unless the claim tool is open; others' claims and viewports keep their current look.
- Mia's review: viewports must not hide under the art; only drafts and claims do. Two custom layers now share one program: `caelestis-presence` below the outline and pixel art, `caelestis-presence-viewports` above the markers and below Wplace's crosshair.
- Presence is world-surface only in this slice. Alliance artboards are a separate canvas and keep no presence socket yet.
- Validation so far: shared presence tests (7), userscript presence client (10) and geometry (8) tests, plus state, layer, manifest, panel, and main suites; userscript `tsc` and ui `svelte-check` clean.
- Codex backend report, verified locally: wire-schema 180 tests, backend 628 tests, backend `tsc`, and biome all pass. Full userscript suite 1340 tests, ui 140 tests, frontend `svelte-check` clean.
- Codex flagged two contract limits: `PresenceRect` cannot express negative alliance HQ coordinates, and read-scoped credentials never see peers because their updates are ignored. Both are acceptable for this world-only slice; alliance presence needs a signed rect later.
- Revocation reaches hibernating presence rooms through the status object: connecting a revocable token records the room key there, and `closeCredential` fans out to it.
- The D1 migration `0021_amazing_blindfold.sql` creates `work_regions`; it applies on deploy and has not run in production yet.
- Not verified in a browser: no running Wplace session with a presence-capable server exists yet. First real check after deploy: two tabs, same server, each should draw the other's dashed viewport and see the headcount in the In progress drawer.
