---
'@caelestis/userscript': patch
---

Prevent template loading and low marker budgets from blocking map frames by removing obsolete bitmap mipmaps and tile-count import limits, bounding overlay uploads, moving contrast outlines below Wplace art, and sampling marker visibility on the GPU.
