# @caelestis/frontend

SvelteKit dashboard for a Caelestis template server.

The dashboard reads manifests and telemetry from `/backend`. Set `VITE_CAELESTIS_SERVER` to use
another full base URL.

Run it with `pnpm --filter @caelestis/frontend dev`.
