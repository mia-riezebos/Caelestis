The pace picker shares the painter picker's searchable, multi-select popover and menu styling. Keep the legend compact; show solid green and red swatches for correct and mismatched pixels. Painter metric controls appear only when painter lines are drawn. Archived progress uses a dashed green line and the same translucent fill as reported progress. Break both at missing snapshots. Archive hover values identify their source and interval, with unknown coverage shown explicitly.
