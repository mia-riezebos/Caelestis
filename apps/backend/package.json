{
  "name": "@wts/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.check.json",
    "db:generate": "drizzle-kit generate",
    "dev": "wrangler dev",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@wts/shared": "workspace:*",
    "drizzle-orm": "0.45.2",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "@types/node": "^26.1.2",
    "@wts/wire-schema": "workspace:*",
    "drizzle-kit": "0.31.10",
    "effect": "4.0.0-beta.102",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  }
}
